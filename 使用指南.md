# WorkBuddy Cockpit 使用指南

> 本地 OpenAI 兼容网关 + Web 管理面板(端口 7863),转发到 WorkBuddy 账号池,按积分轮转账号。
> 当前文档版本:**v1.14.0(2026-09-26)**(版本历史与变更内容见 README「版本」一节)。
>
> 快速上手 / 徽标 / 架构图请看 [README.md](README.md);本篇是本地操作手册。

---

## 1. 启动 / 停止

### 启动

| 方式 | 命令 | 说明 |
|---|---|---|
| **双击 `启动服务.bat`**(推荐) | — | 打开**交互菜单**:`1` 查积分/看账号、`2` 手动批量签到、`3` 手动执行定时任务(子菜单)、`4` 启动服务、`5` 停止、`6` 查看服务状态台账、`7` 看日志、`8` 加入用户(国内版)、`9` 加入国际版用户、`0` 领取国际版加油包、`a` 请求统计(`/v1/stats` 按模型)、`s` 账号运维(停用/恢复/复活)、`q` 退出;选 `4` 时自动检测服务状态(端口+健康检查):运行中可选强制重启、僵死进程自动清理、非本项目占端口则提示;启动前自动编译所需工具 |
| Go 直接运行 | `go run ./cmd/server -config config.json` | 需在项目根目录 |
| 编译 exe（带面板） | 先构建前端（见 README「源码构建」），再 `go build -trimpath -tags embed_panel -o wb2api.exe ./cmd/server` 后运行 | 不占用编译终端 |
| Docker | `docker compose up -d --build` | 本机未装 Docker;需先安装 |

> 启动成功的标志:`loaded N cn account(s) from ./auths` + `workbuddy2api listening on :7863`。

### 停止

| 场景 | 操作 |
|---|---|
| `启动服务.bat` 窗口 | 服务运行中按 `Ctrl+C` → 停服务,回到菜单;菜单里按 `q` 退出窗口 |
| 终端前台运行 | `Ctrl+C` |
| Docker | `docker compose down` |

---

## 2. 配置说明(`config.json`)

> ⚠️ 下方为**示意结构**，`api_key` 请换成你自己的值；`//` 注释仅作说明，JSON 文件里不要写。完整键位以 [`config.example.json`](config.example.json) 为准。

```json
{
  "listen": ":7863",
  "api_key": "your-gateway-key",
  "auth_dir": "./auths",
  "state_file": "./data/state.json",
  "cooldown": {
    "soft_rate": "600s",
    "soft_rate_max": "2h"
  },
  "schedule": {
    "checkin_hours": [9, 21],
    "keepalive_hours": [22]
  },
  "upstream": { "timeout_seconds": 120 },
  "pool":   { "pick_strategy": "weighted" },
  "usage":  { "enabled": true },
  "panel":  { "enabled": true, "loopback_only": false }
}
```

> `cooldown`：`soft_rate` 429 冷却基数（连续触发指数退避）· `soft_rate_max` 退避封顶。历史键 `hard_credit` / `err_threshold` / `err_cooldown` 已退役（硬冷却固定次日 04:00，连败语义并入熔断器），旧配置写了也不报错、自然忽略。
> `schedule`：七类任务各有 `*_hours` 排程与 `*_enabled` 开关（签到/旅行/活跃/保活/开学季/夜猫子/任务执行队列 `queue_*`），详见 `config.example.json`。**排程小时与开关可在面板「设置」或 `PATCH /admin/tasks {kind,hours}` 热改，免重启即时生效**（改动同步深合并写回 config.json）。
> 历史键 `region` 已退役：双域改由账号 realm 与模型名 `global:` 前缀自动路由。
> `pool.pick_strategy`：选号策略，`weighted`(默认,三因子加权随机) / `credits_desc`(余额从大到小严格选号)；面板「设置」可热改，非法值启动即报错。
> 环境变量覆盖：`WB2A_*` 系列（`WB2A_LISTEN`、`WB2A_API_KEY`、`WB2A_AUTH_DIR` 等 28 个）。
>
> **`admin` 管理端点**（可选）：`"admin": {"enabled": true}` 开启后 `api_key` **必填**，否则服务启动即报错退出（`admin.enabled=true 但 api_key 为空`）——空 key 时管理端点等于无鉴权裸奔。开启后可用端点：本地插件面（`/admin/tasks`、`/admin/credits`、`/admin/shutdown` 等，仅本机 loopback 可调）+ 账号运维端点 `/admin/accounts/{uid}/{disable,enable,revive}`（与 `/status` 同 key 鉴权）。Docker 部署时配置位于 `./config/config.json`（compose 目录挂载），容器内脚本经 `WB2A_CONFIG` 环境变量读取该路径。
>
> **`auths` 热加载**：配置了 `auth_dir` 时网关每 5 秒轮询凭证目录，新增账号文件自动进池，免重启。

### ⚠️ 两个密钥的区别(重要)

| 密钥 | 用途 | 说明 |
|---|---|---|
| `config.json` 的 `api_key` | **本地网关鉴权** | 客户端调用时必须带 `Authorization: Bearer <此key>`,否则 `401`;**不会转发给上游** |
| `auths/*.json` 里各账号的 `accessToken` | **上游调用凭证** | 转发时用账号自己的 token + `X-User-Id` 请求 WorkBuddy,消耗**账号积分** |

即:网关 key 是"门禁卡",转发和计费全走账号。两个都要保密。

---

## 3. 账号管理

### 添加账号

**方式一：`启动服务.bat` 菜单**（推荐，Windows 双击即可，无需 bash/python）

双击 `启动服务.bat` → 选 `8 加入用户`（国内版）或 `9 加入国际版用户` → 自动打开浏览器完成授权，登录后自动拉取凭证并落盘到 `auths/`。等待授权期间按**任意键**可中止并返回菜单。

**方式二：命令行**

```bash
cd <项目根目录>
./login.exe join                  # Windows cmd 原生，国内版
./login.exe join --realm=global   # 国际版（workbuddy.ai）
./login.sh                        # bash 环境（交互式选域；脚本在仓库根目录）
```

流程:
1. 自动拉取**授权链接**并尝试打开浏览器(登录用另一个账号)
2. 浏览器完成授权后**自动继续**(无需按 `y`)
3. 自动完成:拉 token → 每日签到(仅国内版,国际版自动跳过) → 落盘到 `auths/workbuddy-<uid>.json`
4. 非 Docker 环境:服务运行中时 `auths` 目录热加载(5s 轮询)自动进池,免重启;未配置 `auth_dir` 的部署才需重启(菜单 `5 停止` → `4 启动服务`)

> **国际版账号**：登录后可用菜单 `0 领取国际版加油包(trial)` 或 `./trial.exe` 领取加油包积分（CN 账号自动跳过）。国际版账号无每日签到，不参与签到/旅行等 CN 定时任务。模型名支持 `global:` 前缀显式指定国际版（如 `global:gpt-5`），不带前缀时由账号池自动路由。

### 查看账号

```bash
# 账号列表(需网关 key)
curl -s http://localhost:7863/status -H "Authorization: Bearer <网关key>"

# 只看某账号的每模型在途台账(该号现在正在跑什么模型)
curl -s http://localhost:7863/status -H "Authorization: Bearer <网关key>" | \
  python -c "import json,sys; d=json.load(sys.stdin); print(json.dumps(d.get('accounts',[{}])[0].get('in_flight_by_model',{}),ensure_ascii=False,indent=2))"

# 积分明细
./credit.exe -pretty              # 美化输出 + 账号明细表
./credit.exe                      # 原始 JSON
```

`/status` 每账号关键字段:`disabled`(系统自动禁用)、`manual_disabled`(手动停用)、`rate_limited_models`(限流模型明细)、`model_costs`(实测成本台账)、`in_flight`(单账号在途请求数)与 `in_flight_by_model`(每模型在途计数,模型 → 计数;归零即删行,只含正在请求的模型,运行态不持久化,重启清零)。

### 轮转规则

三因子加权选号(积分比例 ×10 + 快过期积分占比 ×8 + 闲置补偿,top5 短名单内随机,叠加成本账本择优与 cost_explore 探索);余额不足 → 硬冷却至次日 04:00(不被软冷却/限流覆盖翻型,零余额号不会提前回池);429 → 按上游重置时间指数退避冷却;连败 → 熔断冷却;会话失效(12153) → 禁用。

> **选号策略手动切换**(面板「设置 → 并发与熔断 → 选号策略」下拉,`pool.pick_strategy`,热生效免重启):默认 `weighted` 即上面的智能加权;切 `credits_desc` 则改为**按积分余额从大到小严格选号**——恒取余额最高的账号,最高者冷却/熔断/在途占满才顺延次高,先把大余额号用掉,其余账号近乎闲置。并发突发时同一 100ms 防撞号窗口内的请求会沿余额序下探(第 1 个取最大号、第 2 个取次高……)。冷却/熔断/在途上限/请求级轮换/全冷却兜底均照常生效;排序依据是本地缓存余额(签到/手动刷新回写 + 请求估算扣减,天然产生轮换)。**会话粘性与策略联动**:credits_desc 下粘性的首次分配不再是哈希打散,新会话直接绑当前余额最高的可用号(既有绑定不受影响,绑定号不可用时照常解绑重分配);weighted 下粘性保持原有的哈希打散不变。

> **强制清冷却**(面板账号页按钮 / `POST /api/accounts/{uid}/clear-cooldown`):冷却、熔断、连败降权、6004 模型级限流表一键归零,**不碰禁用两位**(`disabled` / `manual_disabled` 不变)。适用于确认上游已恢复但账号还在冷却期的情况。
>
> **自动解冻口径**(v1.14.0 收敛):签到解冻 / 余额刷新 / 登录回写只清**账号级冷却**(到期时间 / 类型 / 原因),**不重置**软限流退避指数与 6004 模型级限流台账,熔断器亦不动——余额恢复只证明 billing 通道健康,不证明 chat 通道健康。需要全量归零时请用上面的**强制清冷却**按钮。

### 手动执行定时任务(菜单 `3`)

服务的自动排程(签到 9/21 点、旅行、活跃上报、开学季 12 点、夜猫子 1 点、保活 22 点、任务执行队列 10 点默认关)照常跑,`task.exe` 只是**另起进程立即多执行一次**,两者互不影响;幂等(已签到/已领取/当日防抖)由上游与任务体兜底,重复跑安全。触发小时可在面板「设置」页热改,免重启。`task.exe` 的 `kind` 支持 `checkin|activity|keepalive|travel|school|cat|minichat|all`(队列任务在面板「任务中心」触发,不走 task.exe)。

| 子菜单 | 命令 | 说明 |
|---|---|---|
| `1` 令牌保活 | `.\task.exe keepalive` | 按需 refresh 临期 token 并回写 auths |
| `2` 每日签到 | `.\task.exe checkin` | 批量签到(global 账号跳过) |
| `3` 猫猫旅行 | `.\task.exe travel` | 状态机巡检:该派则派、该领则领 |
| `4` 活跃上报 | `.\task.exe activity` | 每号 N 连发(默认 5)+ 达标自动领猫 |
| `5` 开学季任务 | `.\task.exe school` | python 脚本,需本机有 python(`set WB2A_PYTHON=python` 覆盖解释器名) |
| `6` 夜猫子任务 | `.\task.exe cat` | python 脚本,同上 |
| `7` 全部 | `.\task.exe all` | keepalive→checkin→travel→activity→school→cat→minichat 顺序跑 |
| `8` 小程序成长任务 | `.\task.exe minichat` | python 脚本,同上(小程序 Sequential 顺序任务链) |

> 手动执行不写服务的 `logs\server.log`(独立进程),结果直接打在菜单窗口里。

---

## 4. 接口验证

```bash
# 模型列表
curl -s http://localhost:7863/v1/models -H "Authorization: Bearer <网关key>"

# 聊天(流式/工具调用均支持)
curl -s http://localhost:7863/v1/chat/completions \
  -H "Authorization: Bearer <网关key>" \
  -H "Content-Type: application/json" \
  -d '{"model":"auto","messages":[{"role":"user","content":"hi"}]}'

# 健康检查(无需鉴权)
curl -s http://localhost:7863/healthz
```

### 请求统计(2026-09 上游新增)

```bash
# 按模型聚合的请求量/token/缓存/延迟/扣费/积分倍率(需网关 key)
curl -s http://localhost:7863/v1/stats -H "Authorization: Bearer <网关key>"

# 清零统计
curl -s -X POST http://localhost:7863/v1/stats/reset -H "Authorization: Bearer <网关key>"
```

终端表格视图:`go run ./cmd/stats`(等价 JSON:`-json`;原地刷新:`-watch 5s`;按扣费排序:`-sort credits`)。Windows 菜单里选 `a` 即用编译好的 `stats.exe`(首次进菜单自动编译),同一数据源。

### 账号临时停用(可选,需 admin.enabled=true)

```bash
# 停用(对话流量摘除,签到保活照常)/ 恢复 / 复活
curl -s -X POST http://localhost:7863/admin/accounts/<uid>/disable \
  -H "Authorization: Bearer <网关key>" -H "Content-Type: application/json" \
  -d '{"reason":"观察几天"}'
curl -s -X POST http://localhost:7863/admin/accounts/<uid>/enable  -H "Authorization: Bearer <网关key>"
curl -s -X POST http://localhost:7863/admin/accounts/<uid>/revive  -H "Authorization: Bearer <网关key>"

# 强制清冷却(冷却/熔断/连败降权/6004 模型级限流归零,不碰禁用位)
curl -s -X POST http://localhost:7863/api/accounts/<uid>/clear-cooldown \
  -H "Authorization: Bearer <网关key>"

# 账号凭据导出/导入(跨部署迁移;导出与 auths/*.json 落盘同形,同 UID 导入即覆盖更新)
curl -s http://localhost:7863/api/accounts/export -H "Authorization: Bearer <网关key>" -o accounts-export.json
curl -s -X POST http://localhost:7863/api/accounts/import -H "Authorization: Bearer <网关key>" \
  -H "Content-Type: application/json" --data-binary @accounts-export.json
```

> 停用(disable/enable,手动位)与系统自动禁用(revive 清的自动位)是两个独立状态位,`/status` 每账号的 `manual_disabled` / `disabled` 字段可区分。注意 revive(解冻)只清账号级冷却,不重置软限流退避与模型级限流台账、不动熔断器(见 §轮转规则「自动解冻口径」);需要全量归零用 clear-cooldown。

**Windows 菜单入口**:选 `s` 打开账号运维子菜单(list / disable / enable / revive,驱动编译好的 `acct.exe`)。前置条件自动预检:服务需运行中(端点操作的是运行中进程的内存状态,外部直接改 state.json 会被 5s flush 覆盖),且 `config.json` 的 `admin.enabled=true`(未开启会提示后再进)。

### 可用模型

模型目录为**动态探测**（`/v1/models` 实时返回上游实际下发的模型，本地不做静态兜底）。CN 域来自上游模型接口；国际域 `/v3/config` 按 **双 UA 并发探测取并集**（IDE UA 字段权威、CLI UA 补 deepseek 系缺失 id），企业端点补充家族独有模型（如 `gpt-5.3-codex`），试用横幅模型自动补入目录。上下文窗口按「远端权威 → 静态知识表 → model.json 缓存 → models.dev 按需拉取」四级回退。以 `/v1/models` 返回为准。

---

## 5. 接入 cc-switch(Claude Code 使用)

cc-switch 的内置本地代理(127.0.0.1:15721)负责 Anthropic↔OpenAI 转换,workbuddy2api 是 OpenAI 兼容网关,可直接接入。

在 cc-switch「添加 Claude Provider」填:

- **名称**: `workbuddy2api`
- **Base URL**: `http://127.0.0.1:7863/v1`
- **API Key**: `<config.json 里的网关 key>`

**模型映射建议**:

| 插槽 | 模型 |
|---|---|
| Default / Sonnet / Opus / Fable | `deepseek-v4-pro` |
| Haiku / Subagent | `deepseek-v4-flash` |

> 配置方式推荐走 GUI(界面可见可回滚);直接改 `~/.cc-switch/cc-switch.db` 需重启 cc-switch,可能被内存态覆盖。

---

## 5.5 Web 管理面板

浏览器打开 `http://<主机>:7863/` 即面板首页。登录密码 = `config.json` 的 `api_key`(用户名随意填,单凭证体系)。登录失败有限速保护:同一 IP 10 分钟内失败 5 次锁定 15 分钟,忘记 key 时不要在线盲试。

| 路径 | 页面 | 说明 |
|---|---|---|
| `/dashboard` | 总览 | 账号健康(/status 台账)+ 今日用量/扣费;到期积分按天归并口径切换(本地偏好) |
| `/accounts` | 账号池 | 总览/积分条/冷却倒计时;单号复活/禁用/签到/查余额/移除;批量签到/旅行/活跃/保活/余额;**扫码加号**(CN/国际双域 OAuth,免重启入池);**凭据导出/导入**(跨部署迁移账号,同 UID 覆盖更新);**强制清冷却**(冷却/熔断/连败降权/模型级限流一键归零,不碰禁用位) |
| `/models` | 模型 | 模型目录 + 实测输出上限标注(data/output_probes.json) |
| `/playground` | 对话测试台 | 直连本网关 /v1/chat/completions |
| `/tasks` | 任务中心 | 成长任务扫描 + 执行队列(账号内串行、账号间并发 1-3) + 25 个任务动作一键自动化(含小程序 Sequential_Tasks_1..7 顺序任务链:专家对话、5/10 次对话、GLM5.2、灵感功能等,链式依赖每日解锁一环、自动重试) |
| `/activity` | 开学季活动 | 5 任务状态矩阵 + 一键闭环 + 券码 QR 查询(活动期有效) |
| `/stats` | 用量统计 | 逐请求用量分桶(时间片×域×账号×模型,近 24h/72h/7d/30d 数字档 + 「启动以来」全量档,图和表随时间窗同步切换) + /v1/stats 聚合 |
| `/logs` | 日志 | 请求日志(环形缓冲 ~1000 条,重启即清;含 prompt 缓存命中/未命中/写入三段观测与命中率徽标) + 系统日志(任务/对话/系统三频道) |
| `/settings` | 设置 | 配置热编辑(部分键免重启生效,**排程小时热改免重启**) / **选号策略切换**(`weighted` 加权随机 ↔ `credits_desc` 余额降序,热生效) / Upstash 连通性 / 模型映射 / 版本检查 |

**配置开关**(`config.json`):

```json
"usage": { "enabled": true },   // 逐请求用量分桶落盘 data/usage.json(30s 防抖原子写)
"panel": { "enabled": true, "loopback_only": false }  // loopback_only=true 仅限本机访问
```

- `panel.enabled=true` 且 `api_key` 为空 → **拒绝启动**(fail-fast,面板不能裸奔)。
- 配置热编辑:面板里保存的修改会深合并写回 `config.json` 并**保留未知键**;api_key/软冷却/脱敏开关即时生效,池参数/排程小时(`schedule.*_hours`)与排程开关即时生效(排程热改走 `scheduler.SetHours`,改完唤醒调度主循环即时重排定时器),listen/auth_dir/upstream 等装配期字段会提示需重启。
- 模型映射:`/settings` 里可把客户端写死的模型名(如 `gpt-4o`)整名映射到网关模型(如 `cn:glm-5.2`),服务 cc-switch 等不改配置的客户端。

### 账号凭据导出 / 导入(v1.3.0 新增)

面板「账号」页管理栏 **导出 / 导入** 按钮,用于跨部署迁移账号(本机 → 服务器、旧机 → 新机):

- **导出**:`GET /api/accounts/export` 下载一个 JSON 附件,内容与 `auths/*.json` 落盘同形(锁内导出,不夹带半更新 token)。
- **导入**:上传导出文件(或粘贴 JSON),逐账号 `Parse → validUID → BackfillRealm → SaveAtomic → pool.Add`,**同 UID 已存在时覆盖更新**;请求体上限 20MB。
- 导入完成后账号立即热加载进池,免重启;`auths/` 目录同时落盘,重启亦不丢。

### 选号策略切换(面板「设置」页)

面板「设置 → 并发与熔断 → 选号策略」下拉,对应配置键 `pool.pick_strategy`,保存后**热生效免重启**:

- **`weighted`(默认)** — 三因子加权随机选号,行为与历史版本完全一致。
- **`credits_desc`** — 按积分余额**从大到小严格选号**:恒取余额最高账号,最高者冷却/熔断/在途占满才顺延次高;粘性会话的首次分配同样绑当前余额最高号(既有绑定不受影响)。排序依据是本地缓存余额(签到/手动刷新回写 + 请求估算扣减),天然产生轮换。

详见「轮转规则」一节的选号策略说明。

---

## 6. 常见问题排查

| 现象 | 原因 | 解决 |
|---|---|---|
| 聊天返回 `503 no_healthy_account` | 无账号 / 全部冷却禁用 | 加账号或等冷却 |
| 服务启动即退出，日志提示 `admin.enabled=true 但 api_key 为空` | 开启了 admin 管理端点但没配网关 key | 在 config.json 配置 `api_key`，或把 `admin.enabled` 改回 `false` |
| 登录页超时/转圈 | 浏览器代理拦截 `copilot.tencent.com` / `codebuddy.cn` | 无痕窗口;关代理或加白名单 |
| 端口 7863 被占用 | 服务已在运行 | 直接关掉重复的窗口,或先 `Ctrl+C` 停掉旧的 |
| 终端中文乱码 | 控制台编码显示问题 | 不影响功能;服务日志本身正常 |
| `/status` 返回 401 | 网关 key 不对 | 用 `config.json` 的 `api_key` |
| 聊天返回"抱歉,系统检测到您当前输入的信息存在敏感内容..."(任何输入都触发) | 上游内容审核对 system prompt 里的敏感品牌词/客户端特征指纹敏感(见 §7 机制) | 由网关 `internal/upstream/payload.go` 的 `sanitizeSystemPrompts` 自动屏蔽;若新客户端仍报错,按 §8 抓包定位并追加词表 |
| 流式输出格式乱(逐字分行、`●` 符号、错行) | 上游 chunk 含空 `finish_reason:""` 和冗余空字段,中转层(如 cc-switch)转换出错 | 已由 `internal/upstream/sse.go` 的 `normalizeStreamChunk` 规范化;重启服务后重试 |

---

## 7. 敏感内容 / 流式格式 机制说明

`internal/upstream/payload.go` 的 `sanitizeSystemPrompts` 在转发前只对 `role=system` 的消息做词替换(不碰对话内容):

- **敏感品牌词**:`Codex CLI`、`Claude Code`、`Anthropic`、`OpenCode`、`OpenAI`、`ChatGPT` 等
- **客户端特征指纹**:上游会做**精确字符串匹配**识别"这是某客户端(如 Claude Code)的 system prompt"并整段拒绝。实测 `Main branch (you will usually use this for PRs)`(Claude Code gitStatus 快照标志句)单独出现即触发,微调措辞即不触发。词表在 `sensitiveBrandRepl`,新增只需追加一行。

`internal/upstream/sse.go` 的 `normalizeStreamChunk` 在流式透传前删除空值冗余字段(空 `finish_reason`、`reasoning_content`、`function_call`、`refusal`、空 `tool_calls` 等),保持标准 OpenAI SSE 格式,避免中转层渲染错乱。

## 8. 故障诊断方法:定位上游"敏感内容"触发词

当某客户端(Codex / Claude Code / OpenCode / 其他)通过本网关报"敏感内容"被拒,而网关本身(直接 curl 普通请求)正常时,说明触发点在**该客户端的 system prompt 内容**,按以下流程定位:

### 步骤 1:确认是网关还是客户端

```bash
# 直连网关,普通内容
curl http://localhost:7863/v1/chat/completions -H "Authorization: Bearer <key>" \
  -H "Content-Type: application/json" -d '{"model":"deepseek-v4-flash","messages":[{"role":"user","content":"你好"}]}'
```
正常 → 问题在客户端 system prompt;仍拒 → 检查账号/网关配置。

### 步骤 2:抓取真实请求体

用内置开关 `WB_DEBUG_BODY=1` 启动服务,网关会把收到的完整请求体写到 `debug_body.json`(启动目录下):

```bash
# 重启服务时带上环境变量(以 启动服务.bat 为例,先 Ctrl+C 停掉旧的)
set WB_DEBUG_BODY=1 && go run ./cmd/server
```

客户端复现一次 → 得到完整请求体(含 system prompt)。抓完记得去掉环境变量重启,恢复正式模式。

### 步骤 3:用"干净请求"复现

只保留 `messages[0]`(system)+ 首条 user,去掉被污染的历史(tool 调用结果、上次拒绝文案会形成死循环),构造测试请求:
```bash
curl http://localhost:7863/v1/chat/completions -H "Authorization: Bearer <key>" \
  -H "Content-Type: application/json" -d @clean.json | grep finish_reason
# 出现 "content_filter" = 复现成功
```

### 步骤 4:二分定位

把 system prompt 反复切半,分别作为 system 请求,找触发段(`finish_reason: content_filter`)。直到缩小到几十字符,再逐句/逐词替换验证。

**要点**:
- 触发可能需**足够上下文长度**(几十字符的短句不触发,整段才触发)——保持段落在合理长度再切
- 可能是**精确指纹**(整句匹配,微调措辞即不触发)而非关键词
- 验证某词是触发源:替换成中性词后重新测完整请求,通过即确认

### 步骤 5:修复

把确认的触发词/指纹句加入 `internal/upstream/payload.go` 的 `sensitiveBrandRepl`(长词在前),`go test ./internal/...` 通过后重启服务,用真实客户端复测。

---

## 9. 文件结构速览

```
workbuddy-cockpit/
├── 启动服务.bat        # 双击打开交互菜单(查积分/看账号/加入用户/启动服务+状态判断/停止/日志/请求统计/账号运维)
├── config.example.json # 配置模板(复制为 config.json 后填 api_key)
├── config.json         # 网关配置(api_key 在此;不入库/不分发)
├── Dockerfile          # 三段构建(node:22 → golang:1.26 → alpine)
├── docker-compose.yml  # Docker 一键部署
├── web/                # 面板前端源码(Next.js 15,静态导出后 embed)
├── cmd/                # 工具源码
│   ├── server/         # 主服务(网关)
│   ├── credit/         # 积分查询
│   ├── login/          # OAuth 登录
│   ├── acct/           # 账号运维(停用/恢复/复活,走管理端点)
│   ├── stats/          # /v1/stats 请求统计终端视图
│   └── signin/         # 批量签到
├── internal/           # 核心库(auth/pool/scheduler/server/upstream/panel/usage)
├── scripts/            # 辅助脚本(Python 任务体/PowerShell 服务启停)
│   └── start-service.ps1 # Windows 隐藏启动服务(后台独立运行)
├── login.sh 等 *.sh    # bash 版 CLI 登录/签到/积分/加油包/账号运维(根目录)
├── *.exe / signin_bin  # 预编译工具(菜单/脚本自动使用;可删,会重新编译)
├── auths/              # 账号凭证(每个账号一个 json;不入库/不分发)
├── data/state.json     # 轮转状态(冷却/禁用记录;运行时生成)
└── logs/               # 服务日志(运行时生成)
```
